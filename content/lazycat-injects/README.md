# lazycat-injects

`lzc-file-chooser-inject.js` 来自懒猫官方：

https://developer.lazycat.cloud/lazycat-injects/lzc-file-chooser-inject.js

`lzc-build.yml` 的 `contentdir: ./content` 会将本目录打入 LPK，manifest 通过
`file:///lzcapp/pkg/content/lazycat-injects/lzc-file-chooser-inject.js` 引用。
