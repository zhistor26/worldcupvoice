# 参考：lzc-build.yml（目标态 v0.2）
# M1 本地 fat build 可含 images；M4 提审版应移除 images 段，见 IMPLEMENTATION_PLAN §7.2

manifest: lzc-manifest.yml
contentdir: ./content
icon: ./icon.png
pkgout: ./

images:
  web:
    dockerfile: Dockerfile
    build_args:
      NEXT_PUBLIC_AGORA_APP_ID: "{{ params.agora_app_id }}"
      NEXT_AGORA_APP_CERTIFICATE: "{{ params.agora_app_certificate }}"
      NEXT_PUBLIC_LAZYCAT_DEPLOYED: "true"
      NEXT_PUBLIC_REQUIRE_ACCESS_PASSWORD: "false"
  agent:
    dockerfile: server/Dockerfile

# M4 提审 thin release 示例（无 images）：
# manifest: lzc-manifest.yml
# contentdir: ./content
# icon: ./icon.png
# pkgout: ./
# （镜像已 push registry，manifest services.image 指向 registry.lazycat.cloud/...）
